repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: ruff check --fix .
        language: system
        types: [file, python]
      - id: ruff-format
        name: ruff-format
        entry: ruff format .
        language: system
        types: [file, python]
